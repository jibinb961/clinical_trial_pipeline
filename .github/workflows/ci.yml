name: Build and Deploy Prefect Agent to Cloud Run

on:
  push:
    branches:
      - main
      - users/jbaby/google_cloud_integration

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout code
        uses: actions/checkout@v3

      - name: 🔐 Authenticate with Google Cloud
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - name: ☁️ Set up Google Cloud CLI
        uses: google-github-actions/setup-gcloud@v1
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          install_components: 'beta'

      - name: 🐍 Set up Python + Poetry
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: 📦 Install Poetry
        run: |
          curl -sSL https://install.python-poetry.org | python3 -
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          export PATH="$HOME/.local/bin:$PATH"

      - name: 📜 Install project dependencies (for deployment)
        run: |
          poetry install --only main

      - name: 🐳 Authenticate Docker to Artifact Registry
        run: gcloud auth configure-docker us-central1-docker.pkg.dev

      - name: 🔨 Build Docker image
        run: |
          docker build -t us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/clinical-pipeline/prefect-agent:latest .

      - name: 🚀 Push Docker image to Artifact Registry
        run: |
          docker push us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/clinical-pipeline/prefect-agent:latest

      - name: 🛰️ Deploy to Cloud Run
        run: |
          gcloud run deploy prefect-agent \
            --image us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/clinical-pipeline/prefect-agent:latest \
            --platform managed \
            --region us-central1 \
            --memory 2Gi \
            --port 8080 \
            --execution-environment gen2 \
            --set-env-vars PREFECT_API_KEY=${{ secrets.PREFECT_API_KEY }},PREFECT_WORKSPACE=${{ secrets.PREFECT_WORKSPACE }},GCS_BUCKET=${{ secrets.GCS_BUCKET }},GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }},CACHE_DB_PATH=/tmp/drug_cache.sqlite \
            --no-allow-unauthenticated \
            --quiet
